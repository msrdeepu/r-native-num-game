import { View, Text, StyleSheet } from "react-native";

const PrimaryBtn = (props) => {
  return (
    <View>
      <Text>{props.primarybtntext}</Text>
    </View>
  );
};

export default PrimaryBtn;
